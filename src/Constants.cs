namespace Ball;

public static class Dimensions
{
    public const int WindowWidth = 768;
    public const int WindowHeight = 1024;
    public const float AspectRatio = (float)WindowWidth / (float)WindowHeight;
}